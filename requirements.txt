# Core dependencies
# Keep in sync with pyproject.toml [project.dependencies]
pyyaml>=6.0.1
pydantic>=2.4.2
httpx>=0.27.0
python-dotenv>=1.0.1

# Optional provider dependencies
# Install with: pip install switchboard-ai[openai,anthropic]
# openai>=1.40.0
# anthropic>=0.39.0
